@page
@model Licenta.Pages.Marci.EditModel

@{
    ViewData["Title"] = "Editare";
}

<h1>Editare</h1>

<h4>Marcă</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Marca.ID" />
            <div class="form-group">
                <label asp-for="Marca.NumeMarca" class="control-label"></label>
                <input asp-for="Marca.NumeMarca" class="form-control" />
                <span asp-validation-for="Marca.NumeMarca" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvare" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index" class="btn btn-primary">Înapoi</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
